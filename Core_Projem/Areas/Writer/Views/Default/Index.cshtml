@using EntityLayer.Concrete
@model List<Announcement>
@{
	ViewData["Title"] = "Index";
	Layout = "~/Views/Shared/_WriterLayout.cshtml";
}
@{ string deger = null;}
<div class="col-lg-12 grid-margin stretch-card">
	<div class="card">
		<div class="card-body">
			<h4 class="card-title">Duyurular</h4>

			<div class="table-responsive">
				<table class="table">
					<thead>
						<tr>
							
							<th>#</th>
							<th>Başlık</th>
							<th>Tarih</th>
							<th>İçerik</th>
							<th>Tür</th>
							<th>Detaylar</th>
						</tr>
					</thead>
					<tbody>
						@foreach (var item in Model)
						{
							if (item.Status == true)
							{
								deger = "badge badge-success";
							}
							if (item.Status == false)
							{
								deger = "badge badge-danger";
							}
							<tr>
								<td>@item.ID</td>
								<td>@item.Title</td>
								<td>@item.Date.ToString("dd-MM-yyy")</td>
								<td>@item.Content</td>
								<td><label class="@deger">@(item.Status == true ? "Aktif" : "Pasif")</label></td>
								<td><a href="/Writer/Default/AnnouncementDetails/@item.ID/" class="btn btn-outline-primary">Detay</a></td>
							</tr>
						}
					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>

